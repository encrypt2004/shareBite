rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Base rule: require authentication. TODO: add role/ownership checks per collection.
      allow read, write: if request.auth != null;
    }
  }
}
